import {CapacitorConfig} from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ir.mm25zamanian.spy',
  appName: 'spy.mm25zamanian.ir',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
